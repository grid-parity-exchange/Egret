name: test_env
channels:
    - defaults
dependencies:
    - python=3.6
    - numpy
    - scipy
    - pandas
    - pytest
    - six
    - nose
    - matplotlib
    - seaborn
    - pip:
        - coveralls
